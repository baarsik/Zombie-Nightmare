/* dev.cspirate.ru */

#if defined _jb_included
	#endinput
#endif
#define _jb_included

// If user is simon returns true
native jb_get_user_simon(id)

// If user will get freeday on next spawn returns true
native jb_get_user_freeday_ns(id)

// Sets user auto freeday on next spawn
native jb_set_user_freeday_ns(id)