/*ringmod.ucoz.com*/
#pragma reqlib "ringf"
/* Returns 1 if user is a samara */
native ring_get_samara(index)
/* Returns 1 if user is a hero */
native ring_get_hero(index)
/* ring gets unpain */
native ring_unpain(index)